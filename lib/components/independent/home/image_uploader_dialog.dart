import 'package:flutter/material.dart';

class ImageUploaderDialog extends StatelessWidget {
  const ImageUploaderDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}